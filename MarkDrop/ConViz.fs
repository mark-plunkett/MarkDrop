module ConViz

    //let displayWaveForm samples =

    //    let c = new Canvas(100, 100)
    //    for i in [0..100] do
    //        for j in [0..100] do

    //            if i % 2 = 0 && j % 3 = 0 then c.Set(i, j)

    //    System.Console.Write(c.Frame())